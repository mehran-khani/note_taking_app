import 'package:flutter/foundation.dart';

@immutable
class AuthTexts {
  static const accountExists = 'account-exists-with-different-credential';
  static const googleCome = 'google.com';
  static const email = 'email';

  const AuthTexts._();
}
